spring:
  ### DB ?? ###
  datasource:
    url: jdbc:mysql://localhost:3306/job-gsm
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  ### JPA ?? ###
  jpa:
    database-platform: org.hibernate.dialect.MariaDB103Dialect
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    #        hbm2ddl:
    #          auto: update
    show-sql: true
